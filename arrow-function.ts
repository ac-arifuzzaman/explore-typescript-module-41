// normal function
function added(num1: number, num2: number) {
    return num1 + num2;
}

// arrow function
const add2 = (num1: number, num2: number): number => num1 + num2;

const doMath = (x: number, y: number): void => {

}

// object declare 
const students: string[] = ['jodu', 'kodu', 'nodu', 'godu']